if (ef1_a == 1)
    新建文件
    并将内容打印到文件
    执行rpb1
    if (rpb1 == 1)
        并将内容打印到上一文件
        if(rpb2==1)
            并将内容打印到上衣一文件
            执行its
            if（its == 1）
                将内容打印到上一文件
        else：
            执行its
    else :
        执行rpb2
        if (rpb2 == 1)
            并将内容打印到上一文件
            if (its == 1)
                并将内容打印到上衣一文件

        else：
            执行its
            if (its == 1)
                并将内容打印到上衣一文件

else：
    执行rpb1
    if(rpb1==1)
        新建文件
        并将内容打印
        if (rpb2 == 1)
            新建文件
            并将内容打印
            执行its
            if（its == 1）
                将内容打印上一文件
        else
            执行its
            if（its == 1）
                将内容打印到上一文件

    else：
        执行rpb1
        if(rpb2==1)
            新建文件
            并将内容打印
            if（its == 1）
                将内容打印
        else：
            执行its
            if(its ==1)
                新建文件
                并将内容打印
            else：
                pass